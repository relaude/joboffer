@attribute [Authorize(Roles = JOUSerRole.HROD)]
@attribute [Route(JORoutes.HROD.Approvals)]

<JobOffersByStatus 
    StatusId="@JOMainStatus.ForApproval" 
    RedirectTo="@JORoutes.HROD.Approve"
    HeaderTitle="HROD-Approvals"/>

@code {

}
