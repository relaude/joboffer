@attribute [Authorize(Roles = JOUserRole.TA)]
@attribute [Route(JORoutes.TA.Return)]

<JobOffersByStatus 
    StatusId="@JOMainStatus.Returned"
    RedirectTo="@JORoutes.TA.Return"
    HeaderTitle="Returned Job Offer" />

@code {

}
