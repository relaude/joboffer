@attribute [Authorize(Roles = JOUSerRole.TA)]
@attribute [Route(JORoutes.TA.Accept)]

<JobOffersByStatus 
    StatusId="@JOMainStatus.Emailed"
    RedirectTo="@JORoutes.TA.Accept"
    HeaderTitle="Job Offer Accept" />

@code {

}
