@attribute [Authorize(Roles = JOUserRole.PEHead)]
@attribute [Route(JORoutes.PEH.JobOfferDetails + "/{jobOfferId:int}")]

<JOAnalysis jobOfferId="jobOfferId" GoBackUrl="@JORoutes.PEH.JOForApproval" />

@code {
    [Parameter] public int jobOfferId { get; set; }
}
