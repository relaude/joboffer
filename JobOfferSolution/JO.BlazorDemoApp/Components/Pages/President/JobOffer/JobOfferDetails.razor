@attribute [Authorize(Roles = JOUserRole.President)]
@attribute [Route(JORoutes.President.JobOfferDetails + "/{jobOfferId:int}")]

<JOAnalysis jobOfferId="jobOfferId" GoBackUrl="@JORoutes.President.JOForApproval" />

@code {
    [Parameter] public int jobOfferId { get; set; }
}
