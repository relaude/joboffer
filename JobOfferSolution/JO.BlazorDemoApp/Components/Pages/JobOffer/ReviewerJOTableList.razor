<div class="card shadow mb-4 small">
    <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
        <h6 class="jo-title">@Title</h6>
    </div>

    <div class="card-body">
        <div class="table-responsive">
            <table class="jo-table">
                <thead>
                    <tr>
                        <th>Reference Number</th>
                        <th>Candidate ID</th>
                        <th>Candidate</th>
                        <th>Status</th>
                        <th>Workflow Status</th>
                        <th>Created</th>
                        <th>Modified</th>
                    </tr>
                </thead>
                <tbody>
                    @if (JobOffers.Count == 0)
                    {
                        <tr><td colspan="7" class="text-center text-muted py-4" role="status">No job offers match the selected filter.</td></tr>
                    }
                    @foreach (var joDboxCandidate in pagedJobOffers.Data)
                    {
                        <tr @key="joDboxCandidate.Id">
                            <td>
                                <a href="@SetJOlink(joDboxCandidate)">
                                    <strong>@(joDboxCandidate.RefNum)</strong>
                                </a>
                            </td>
                            <td>@joDboxCandidate.DboxRefNum</td>
                            <td>@joDboxCandidate.CandidateName</td>
                            <td>@joDboxCandidate.ActionByName</td>
                            <td>@joDboxCandidate.FlowName</td>
                            <td>
                                <div>@joDboxCandidate.CreatedByName</div>
                                <small class="text-muted">@(joDboxCandidate.CreatedAt?.ToString("dd MMM yyyy, HH:mm") ?? "-")</small>
                            </td>
                            <td>
                                <div>@joDboxCandidate.ModifiedByName</div>
                                <small class="text-muted">@(joDboxCandidate.ModifiedAt?.ToString("dd MMM yyyy, HH:mm") ?? "-")</small>
                            </td>
                        </tr>
                    }
                </tbody>
                <tfoot>
                    <tr>
                        <td colspan="7">
                            <Pagination Result="@pagedJobOffers"
                                        PageChanged="ChangePage"
                                        PageSizeChanged="ChangePageSize"
                                        AriaLabel="Job offer pages" />
                        </td>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div>
