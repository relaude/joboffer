@attribute [Authorize(Roles = JOUserRole.TALead)]
@attribute [Route(JORoutes.TALead.JobOfferDetails + "/{jobOfferId:int}")]

<JobOfferAnalysis jobOfferId="jobOfferId" GoBackUrl="@JORoutes.TALead.JOForReview" />

@code {
    [Parameter] public int jobOfferId { get; set; }
}
