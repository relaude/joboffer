@* @attribute [Authorize(Roles = JOUserRole.TA)]
@attribute [Route(JORoutes.TALead.JobOfferDetails + "/{jobOfferId:int}")] *@

<JOAnalysis jobOfferId="jobOfferId" GoBackUrl="@JORoutes.TALead.JOForReview" />

@code {
    [Parameter] public int jobOfferId { get; set; }
}
