@using JO.BlazorDemoApp.Components.Pages.Candidate
@using JO.BlazorDemoApp.Components.Pages.Pagination
@attribute [Authorize(Roles = JOUserRole.TALead)]
@attribute [Route(JORoutes.TALead.Candidates)]

<h1 class="h3 mb-4 text-gray-800">TA Lead Candidates</h1>

<CandidateKpiBoxes Total="@total"
                   WithResponseCount="@withResponse"
                   NoResponseCount="@withOutResponse"
                   CreatedCount="@joCreated"
                   DraftCount="@joDraft"
                   ForCreationCount="@forJOCreation"
                   OnFilterChanged="FilterCandidates" />

<CandidateTableList Candidates="@pagedCandidates.Data"
                    RoleId="@JOUserRole.TALead">
    <FooterTemplate>
        <Pagination Result="@pagedCandidates"
                    PageChanged="ChangePage"
                    PageSizeChanged="ChangePageSize"
                    AriaLabel="Candidate pages" />
    </FooterTemplate>
</CandidateTableList>
